{% for item in items %}
  {##
   # Example: Add icons to buttons
   #
  {% set title = {
    '#type': 'inline_template',
    '#template': '{{ original_title }} {{ icon|raw }}',
    '#context': {
      'original_title': item.content['#title'],
      'icon': source('@lucide/arrow-right.svg'),
    }

  } %}
  {% set item = item|set('content.#title', title) %}
   #
   # End example
   ##}
  {{ item.content }}
{% endfor %}

