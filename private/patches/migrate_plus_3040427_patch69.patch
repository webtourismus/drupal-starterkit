diff --color -ruN a/src/Plugin/migrate/source/Url.php b/src/Plugin/migrate/source/Url.php
--- a/src/Plugin/migrate/source/Url.php	2026-01-09 14:02:08
+++ b/src/Plugin/migrate/source/Url.php	2026-01-09 14:02:30
@@ -4,6 +4,7 @@

 namespace Drupal\migrate_plus\Plugin\migrate\source;

+use Drupal\Component\Plugin\Exception\InvalidPluginDefinitionException;
 use Drupal\Core\Plugin\ContainerFactoryPluginInterface;
 use Drupal\migrate\Attribute\MigrateSource;
 use Drupal\migrate\Plugin\MigrationInterface;
@@ -13,6 +14,43 @@
 /**
  * Source plugin for retrieving data via URLs.
  *
+ * Available configuration keys:
+ * - urls: (Required) A list of source URLs to retrieve.
+ *   - Static URLs can be provided as a string or array of strings.
+ *   - A callback function can be provided to dynamically generate URLs.
+ *     The callback receives the MigrationInterface object and should return
+ *     an array of URLs.
+ *
+ * Example with static URLs:
+ * @code
+ * source:
+ *   plugin: url
+ *   data_fetcher_plugin: http
+ *   data_parser_plugin: json
+ *   urls:
+ *     - 'https://example.com/api/items'
+ *   item_selector: /data
+ * @endcode
+ *
+ * Example with callback:
+ * @code
+ * source:
+ *   plugin: url
+ *   data_fetcher_plugin: http
+ *   data_parser_plugin: json
+ *   urls:
+ *     callback: my_module_get_migration_urls
+ *   item_selector: /data
+ * @endcode
+ *
+ * The callback function signature:
+ * @code
+ * function my_module_get_migration_urls(\Drupal\migrate\Plugin\MigrationInterface $migration): array {
+ *   // Return array of URLs to process.
+ *   return ['https://example.com/api/item/1', 'https://example.com/api/item/2'];
+ * }
+ * @endcode
+ *
  * @MigrateSource(
  *   id = "url"
  * )
@@ -44,9 +82,22 @@
     MigrationInterface $migration,
     protected DataParserPluginManager $parserPluginManager,
   ) {
-    if (!is_array($configuration['urls'])) {
+    // Handle callback-based URL generation.
+    if (is_array($configuration['urls']) && isset($configuration['urls']['callback'])) {
+      $callback = $configuration['urls']['callback'];
+      if (!is_callable($callback)) {
+        throw new InvalidPluginDefinitionException(
+          $plugin_id,
+          sprintf('The "urls" callback "%s" is not callable.', $callback)
+        );
+      }
+      $configuration['urls'] = call_user_func($callback, $migration);
+    }
+    // Normalize single URL to array.
+    elseif (!is_array($configuration['urls'])) {
       $configuration['urls'] = [$configuration['urls']];
     }
+
     parent::__construct($configuration, $plugin_id, $plugin_definition, $migration);

     $this->sourceUrls = $configuration['urls'];
diff --color -ruN a/src/Plugin/migrate_plus/data_parser/Json.php b/src/Plugin/migrate_plus/data_parser/Json.php
--- a/src/Plugin/migrate_plus/data_parser/Json.php	2026-01-09 14:02:09
+++ b/src/Plugin/migrate_plus/data_parser/Json.php	2026-01-09 14:03:20
@@ -45,12 +45,13 @@
    *
    * @param string $url
    *   URL of a JSON feed.
-   * @param string|int $item_selector
+   * @param string|int|bool $item_selector
    *   Selector within the data content at which useful data is found.
+   *   Set to FALSE to return the entire source data as a single item.
    *
    * @throws \GuzzleHttp\Exception\RequestException
    */
-  protected function getSourceData(string $url, string|int $item_selector = '') {
+  protected function getSourceData(string $url, string|int|bool $item_selector = '') {
     // Use cached source data if this is the first request or URL is same as the
     // last time we made the request.
     if ($this->currentUrl != $url || !$this->sourceData) {
@@ -67,6 +68,12 @@
         $this->sourceData = json_decode($utf8response, TRUE);
       }
       $this->currentUrl = $url;
+    }
+
+    // If item_selector is explicitly FALSE, return the entire source data
+    // as a single item to import.
+    if ($item_selector === FALSE) {
+      return [$this->sourceData];
     }

     // Backwards-compatibility for depth selection.
